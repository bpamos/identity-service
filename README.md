# identity-service

Auth/identity service that issues and introspects bearer tokens for internal APIs. Also exposes a static key-metadata stub for clients that cache verification material.

This repository is a **demo fixture** for the Fleet CVE Remediation exercise. It is not a production service.
